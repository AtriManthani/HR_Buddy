/**
 * Home page — Server Component shell.
 * All interactivity lives inside ChatShell (client boundary).
 */

import ChatShell from "@/components/layout/ChatShell";

export default function HomePage() {
  return <ChatShell />;
}
